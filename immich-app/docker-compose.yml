services:
  immich-server:
    container_name: immich_server
    image: ghcr.io/immich-app/immich-server:${IMMICH_VERSION:-release}
    # extends:
    #   file: hwaccel.transcoding.yml
    #   service: cpu # set to one of [nvenc, quicksync, rkmpp, vaapi, vaapi-wsl] for accelerated transcoding
    volumes:
      # Do not edit the next line. If you want to change the media storage location on your system, edit the value of UPLOAD_LOCATION in the .env file
      - ${UPLOAD_LOCATION}:/data
      - /home/hyperv/volumes/CHANHOMEDS/family_media_archive/cellphone-photo-backup/Ethan-iPhone-Backup:/home/ethan/photos:ro
      #- /home/hyperv/volumes/ssd-200gb/Ethan-iPhone-Backup:/home/ethan/photos
      - /home/hyperv/volumes/CHANHOMEDS/family_media_archive/cellphone-photo-backup/Flora-iPhone-Backup:/home/flora/photos:ro
      - /home/hyperv/volumes/CHANHOMEDS/family_media_archive/cellphone-photo-backup/Sidney-iPhone-Backup:/home/sidney/photos:ro
      - /home/hyperv/volumes/CHANHOMEDS/family_media_archive/cellphone-photo-backup/Larrisa-iPhone-Backup:/home/larrisa/photos:ro
      - /home/hyperv/volumes/CHANHOMEDS/family_media_archive/camera-media:/home/nas/photos:ro
      - /home/hyperv/docker/immich-app/kiely-phone-backup:/home/kiely/photos
      - /etc/localtime:/etc/localtime:ro
    env_file:
      - .env
    expose:
      - "2283"
    networks:
      - caddy-internal-bridge # To be accessible via Caddy reverse proxy
      - immich-internal-bridge
    #ports:
    #  - "2283:2283"
    depends_on:
      - immich-redis
      - immich-postgres
    restart: always
    healthcheck:
      disable: false

  immich-machine-learning:
    container_name: immich_machine_learning
    # For hardware acceleration, add one of -[armnn, cuda, rocm, openvino, rknn] to the image tag.
    # Example tag: ${IMMICH_VERSION:-release}-cuda
    image: ghcr.io/immich-app/immich-machine-learning:${IMMICH_VERSION:-release}
    # extends: # uncomment this section for hardware acceleration - see https://docs.immich.app/features/ml-hardware-acceleration
    #   file: hwaccel.ml.yml
    #   service: cpu # set to one of [armnn, cuda, rocm, openvino, openvino-wsl, rknn] for accelerated inference - use the `-wsl` version for WSL2 where applicable
    volumes:
      - ./model-cache:/cache
    env_file:
      - .env
    restart: always
    networks:
      - immich-internal-bridge
    healthcheck:
      disable: false

  immich-redis:
    container_name: immich_redis
    image: docker.io/valkey/valkey:9@sha256:fb8d272e529ea567b9bf1302245796f21a2672b8368ca3fcb938ac334e613c8f
    healthcheck:
      test: redis-cli ping || exit 1
    networks:
      - immich-internal-bridge
    restart: always

  immich-postgres:
    container_name: immich_postgres
    image: ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0@sha256:bcf63357191b76a916ae5eb93464d65c07511da41e3bf7a8416db519b40b1c23
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_DB: ${DB_DATABASE_NAME}
      POSTGRES_INITDB_ARGS: "--data-checksums"
      # Uncomment the DB_STORAGE_TYPE: 'HDD' var if your database isn't stored on SSDs
      # DB_STORAGE_TYPE: 'HDD'
    volumes:
      # Do not edit the next line. If you want to change the database storage location on your system, edit the value of DB_DATA_LOCATION in the .env file
      - ${DB_DATA_LOCATION}:/var/lib/postgresql/data
    networks:
      - immich-internal-bridge
    shm_size: 128mb
    restart: always

volumes:
  model-cache: #immich-asset

networks:
  immich-internal-bridge:
    driver: bridge
